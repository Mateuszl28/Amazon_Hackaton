<?xml version="1.0" encoding="utf-8"?>
<!-- Dev: allow plain HTTP to the local backend on the LAN. Production uses the HTTPS API Gateway URL. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
